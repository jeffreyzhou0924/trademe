# 数据库配置（统一使用 SQLite）
# 注意：各服务在各自目录下使用各自的 .env 文件。
# - 用户服务（Prisma / backend/user-service）：
#   DATABASE_URL 使用 Prisma 的 sqlite 连接串格式：file: 路径
# - 交易服务（SQLAlchemy / backend/trading-service）：
#   DATABASE_URL 使用 sqlite+aiosqlite 连接串格式

# 用户服务（Prisma）
# 在 backend/user-service/.env 中设置：
# DATABASE_URL=file:./data/trademe.db

# 交易服务（FastAPI）
# 在 backend/trading-service/.env 中设置：
# DATABASE_URL=sqlite+aiosqlite:///./data/trademe.db

REDIS_URL=redis://localhost:6379

# JWT配置
JWT_SECRET=your_super_secret_jwt_key_here_min_32_chars
JWT_REFRESH_SECRET=your_super_secret_refresh_key_here_min_32_chars
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=30d

# Google OAuth
GOOGLE_CLIENT_IDV=832153922895-6hc3h1qsb72td6gd8lq9vcajq3rborpj.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret

# AI服务配置
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key

# 交易所API配置（用于平台服务，非用户密钥）
BINANCE_API_KEY=your_binance_api_key
BINANCE_SECRET_KEY=your_binance_secret_key
OKX_API_KEY=76ba9b3a-38b6-4ed3-9ce7-44d603188b13
OKX_SECRET_KEY=4021858325F5A3BEC3F64B6D0533E412
OKX_PASSPHRASE=Woaiziji..123

# 加密密钥（用于加密用户API密钥）
ENCRYPTION_KEY=your_32_character_encryption_key

# 邮件服务配置
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=jeffreyzhou0924@gmail.com
SMTP_PASSWORD=bhir hrzg xoqc caxm

# 支付配置
BTC_RPC_URL=https://blockstream.info/api
USDT_RPC_URL=https://api.trongrid.io
PAYMENT_WALLET_ADDRESS=your_payment_wallet_address

# 服务端口配置
USER_SERVICE_PORT=3001
TRADING_SERVICE_PORT=8001
AI_SERVICE_PORT=8002
MARKET_SERVICE_PORT=8003

# 前端配置
NEXT_PUBLIC_API_URL=http://localhost/api/v1
NEXT_PUBLIC_WS_URL=ws://localhost/ws

# 日志配置
LOG_LEVEL=info
LOG_FILE=logs/app.log

# 开发环境配置
NODE_ENV=development
PYTHON_ENV=development
